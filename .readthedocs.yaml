version: 2

submodules:
  include:
    - shared
  recursive: true

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs

sphinx:
  configuration: docs/conf.py
